to open the `init.lue` file

```
nvim -c 'edit $MYVIMRC'
```

```
nvim --headless -c 'echo stdpath("config")' -c 'echo ""' -c 'quit'
```

```
nvim --headless -c 'echo stdpath("data")' -c 'echo ""' -c 'quit'
```
